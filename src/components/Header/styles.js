import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    header: {
        height: '10%',
        width: '100%',
        borderBottomColor: "black",
        borderBottomWidth: 1,
    },
    headerTitle: {
        fontSize: 25,
        fontWeight: 'bold',
        marginLeft: '4%',
        color: 'black',
    }
});