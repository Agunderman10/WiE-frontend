import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    groupHeader: {
        position: "relative",
        fontSize: 20,
        fontWeight: 'bold',
        color: "black",
        marginLeft: '4%',
        marginTop: '3%',
        marginBottom: '1%',
    },
});